services:
  backend:
    build:
      context: ./backend
    container_name: grid-backend
    volumes:
      - grid_data:/data
      - ./backend:/app
      - ./backend/sample_data:/app/sample_data
    environment:
      - BOOTSTRAP_DATA=${BOOTSTRAP_DATA:-true}
      - DB_PATH=/data/grid_data_cim.duckdb
      - PARQUET_DIR=/data/cim_readings
      - CIM_MODEL_PATH=/app/sample_data/IEEE8500.xml
      - WEATHER_DATA_PATH=/app/sample_data/weather.epw
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2.0'

  frontend:
    build:
      context: ./frontend
    container_name: grid-frontend
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    depends_on:
      - backend
    restart: unless-stopped

  documentation:
    build:
      context: ./docs
    container_name: grid-docs
    ports:
      - "${WEBSITE_PORT:-3000}:3000"
    restart: unless-stopped

  generator:
    build:
      context: ./backend
    container_name: grid-generator
    volumes:
      - grid_data:/data
      - ./backend:/app
      - ./backend/sample_data:/app/sample_data
    environment:
      - DB_PATH=/data/grid_data_cim.duckdb
      - PARQUET_DIR=/data/cim_readings
      - CIM_MODEL_PATH=/app/sample_data/IEEE8500.xml
      - WEATHER_DATA_PATH=/app/sample_data/weather.epw
    entrypoint: [ "sh", "./scripts/bootstrap.sh" ]
    deploy:
      resources:
        limits:
          cpus: '2.0'

volumes:
  grid_data:
